import defaultSingle from '../templates/single/default.js';
import defaultChain from '../templates/chain/default.js';

export const singleRegistry = {
  default: defaultSingle
};

export const chainRegistry = {
  default: defaultChain
};
